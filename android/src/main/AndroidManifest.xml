<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Intentionally empty. The native Colada Android SDK contributes its own
         permissions and components through manifest merging; this bridge adds
         none of its own. -->
</manifest>
