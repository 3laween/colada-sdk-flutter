/// Colada Flutter SDK — mobile attribution and event tracking.
///
/// This package is a bridge over the native Colada Android and iOS SDKs. It is
/// the single public entry point for the package; everything under `src/` is
/// private and must never be imported directly.
///
/// The public API (the `Colada` facade, events, attribution models, exceptions
/// and logging) is introduced in Phase 3. This file intentionally exports
/// nothing yet — the package skeleton is proven to build and test green before
/// any Colada functionality exists.
library;
