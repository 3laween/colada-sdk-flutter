import 'package:flutter_test/flutter_test.dart';

void main() {
  // Phase 1 skeleton: the public API arrives in Phase 3, and its unit tests
  // arrive with it. This placeholder only proves the `flutter test` job is
  // wired and green so every later commit is checked from here on.
  test('CI test harness is wired', () {
    expect(2 + 2, 4);
  });
}
